# 个人财务分析 AI Agent — 简历技能提炼

> 项目规模：~4,800 行生产代码 + 55 条 pytest 测试 | 10 个核心模块 | 从零手写，零 Agent 框架依赖

---

## 一、LLM Agent 架构设计

### 1. 从零手写 ReAct 推理-行动循环
- 基于 Anthropic Messages API 实现完整的 Think → Act → Observe 循环，不依赖 LangChain / AutoGen / CrewAI 等任何 Agent 框架
- 支持**并发工具执行**：LLM 单轮返回多个 tool_use 时，通过 `asyncio.gather` 并行调用，减少用户等待时间
- **双 LLM 客户端架构**：主 Agent 与 Critic/Reflection 评审器使用独立 Anthropic Client，避免评审 Prompt 污染主 Agent 上下文窗口
- **隐藏参数绑定**：通过 `functools.partial` 将 `_client`、`user_id` 等敏感参数在注册时绑定，LLM 永远看不到这些参数，防止幻觉注入
- 安全上限机制：最大 10 轮推理循环，超出后优雅降级

### 2. 工具系统设计（8 个工具）
- 设计框架无关的工具注册表：工具函数、注册表映射、Anthropic Tool Schema 三层分离，无继承、无装饰器魔法
- 财务计算器的安全沙箱：字符白名单 + `eval(..., {"__builtins__": {}})` 禁止任意代码执行
- 预算方案生成管道：关键词初始比例 → 确定性计算 → LLM Critic 外部审查（最多 2 轮修订）→ LLM 自我反思 → 跨版本质量对比
- 工具的 `_client=None` 参数兼作 Feature Flag：传 None 时跳过整个 LLM 评审管道（用于 A/B 对照实验）

### 3. 跨会话记忆系统（Mem0）
- 渐进式信息披露架构：每轮对话注入 ~100 token 记忆摘要到 System Prompt，完整记忆按需通过 `memory_recall` 工具检索
- 记忆写入采用 fire-and-forget 异步模式（`asyncio.create_task`），不阻塞用户响应
- 解决 Mem0 嵌入维度不匹配问题：显式配置 Qdrant `embedding_model_dims: 512` 以匹配 `bge-small-zh-v1.5`

---

## 二、Agentic RAG 检索增强生成

### 1. 生产级两阶段检索管道
- **粗排**：Qdrant 向量召回（top_k=20，Cos相似度，FastEmbed bge-small-zh-v1.5）
- **精排**：Cross-Encoder 重排序（top_n=5，bge-reranker-base）
- 自定义递归语义分块器：优先级为段落 → 句子 → 从句 → 硬截断，支持 overlap 保证上下文连续性
- 支持动态向量维度检测：探针模式自动适配嵌入模型维度，换模型无需改配置
- 线程安全单例模式（`threading.Lock`），适配 FastAPI 多 worker 并发
- 幂等文档上传：按 user_id 检测同名文档，自动替换旧分块而非创建重复

### 2. Agentic RAG — 路由器 + 评审器 + 自动改写
- **QueryRouter**：基于正则的分类器，将查询分为 exact（精确数据）和 summary（综合概述），分别应用不同检索策略（top_k=20/30, top_n=5/7）
- **RetrievalCritic**：检索后自动评估质量（双阈值：avg≥0.3, max≥0.5），不达标时触发自动改写重试（缩短查询 或 注入金融领域关键词）
- 路由器与评审器完全封装在 `retrieve_document` 工具内部，Agent 的 ReAct 循环对其无感知——可独立升级检索策略

### 3. RAGAS 定量评估体系
- 设计对照实验：Baseline RAG（固定参数）vs Agentic RAG（Router + Critic + Reformulation），同一生成器、同一 28 组 QA 对
- 4 维 RAGAS 指标评估：faithfulness, answer_relevancy, context_precision, context_recall
- **预评估验证脚本**（`ragas_eval_verify.py`）：在消耗 ~112 次 LLM 调用之前，先验证文档可检索性、检索覆盖率和生成器可用性，避免浪费 API 费用
- 自动生成 A/B 对比报告：分维度增量表格 + 解读建议
- QA 对按难度分层：直接查找（8对）、多段落综合（10对）、诚实性探测（10对——答案不在文档中，测试拒答能力）

---

## 三、LLM 评估体系

### LLM-as-Judge 评测
- **双重评估方法**：数值正确性用硬编码正则校验（不信任 LLM 做算术），主观维度用 LLM 结构化评分（1-5 分，三维度：储蓄合理性 / 建议可操作性 / 目标匹配度）
- 通过 tool-use 强制结构化输出：评委模型必须调用 `submit_score` 工具，确保输出可解析、有逐维度的评分理由
- 10 个测试用例的受控 A/B 对比：同一用例生成有/无 Reflection 两份方案，计算分维度均值增量

### Reflection 自我修正
- 实现双 Critic 架构：外部 Critic（建议数值参数调整）+ 自我反思（审视逻辑一致性和目标匹配度）
- `_compare_plans()` 智能版本选择：储蓄率合理性范围检查 + 净改善率显著性阈值（2%），避免噪音误判
- 诚实标签：`_label_from_actual()` 报告真实达成率而非名义目标值

---

## 四、可观测性工程

### 零开销 LangFuse 追踪
- 条件装饰器模式：LangFuse 未配置时 `@traced()` 返回透传函数（`lambda fn: fn`），零性能开销、零崩溃风险
- 完整的调用链追踪：HTTP 中间件 → agent.chat → react.iteration_N → tool.execute → 子操作，嵌套 Span 跨异步边界传播
- PII 脱敏管道：5 条正则规则（邮箱、手机号、身份证号、Mem0 Key、API Key），所有内容在发送到 LangFuse 前先脱敏
- 全链路优雅降级：每个 LangFuse 调用都包裹在 `try/except: pass` 中，可观测平台宕机不影响业务

---

## 五、工程化能力

### 测试（55 条 pytest 测试用例）
- 覆盖 14 个纯函数 + 5 个 Pydantic 校验器，含参数化边界测试、边界条件（零收入、空数组）、负面测试
- 测试辅助工厂函数减少样板代码

### 数据校验（Pydantic v2）
- `field_validator`：金额自动四舍五入、储蓄率范围检查
- `model_validator`：跨字段不变量（比例和 ≈ 1.0、ok=False 必须带 issues）

### 代码重构
- 消除 4 个旧版/重复文件（786 行死代码）
- 抽离 5 个硬编码 API Key 为环境变量，设计合理的默认值和回退链

### FastAPI 服务化
- 异步端点 + LangFuse HTTP 中间件 + 会话管理（内存级）
- `asyncio.to_thread()` 将同步 PDF 解析隔离到线程池，防止阻塞事件循环

---

## 六、技术栈速览

| 层级 | 技术 |
|------|------|
| LLM | Anthropic SDK（兼容 DeepSeek API）、DeepSeek-v4-pro |
| Agent 架构 | 手写 ReAct 循环、asyncio 并发、双客户端 |
| RAG | Qdrant 向量库、FastEmbed（bge-small-zh-v1.5 + bge-reranker-base）、递归语义分块 |
| 记忆 | Mem0（双 Qdrant 集合 + SQLite 历史） |
| 数据 | Pydantic v2（field_validator + model_validator） |
| 服务 | FastAPI + uvicorn |
| 可观测 | LangFuse（零开销条件追踪 + PII 脱敏） |
| 评估 | RAGAS 4 指标 + LLM-as-Judge 三维评分 |
| 测试 | pytest（55 条，参数化 + 边界 + 负面测试） |
| 工程 | Git Conventional Commits、环境变量配置、代码重构 |
