# 项目简介 — 个人财务分析 AI Agent

---

## 一句话版（适合简历头部 / LinkedIn 标题）

> 从零构建的智能财务分析 Agent，手写 ReAct 推理循环、Agentic RAG 检索增强、LLM 自我反思与可观测性体系，4,800 行生产级代码，零 Agent 框架依赖。

---

## 段落版（适合简历项目栏，~150 字）

**个人财务分析 AI Agent** | Python · Anthropic SDK · Qdrant · FastAPI · LangFuse

独立设计并实现了一个完整的 AI Agent 系统，核心亮点包括：

- **从零手写 ReAct 推理-行动循环**，基于 Anthropic Messages API，支持并发工具调用与双 LLM 客户端架构，不依赖 LangChain 等任何 Agent 框架
- **Agentic RAG 检索管道**：两阶段检索（向量粗排 + Cross-Encoder 精排）+ QueryRouter 查询分类 + RetrievalCritic 自动质控与改写重试，通过 RAGAS 4 维指标定量评估
- **LLM 自我反思机制**：双 Critic 架构（外部审查 + 自我反思）+ LLM-as-Judge 三维评分（1-5 分），10 组 A/B 对照实验验证改善效果
- **工程化完备**：55 条 pytest 测试、Pydantic v2 数据校验、LangFuse 全链路追踪+PII 脱敏、Mem0 跨会话记忆、FastAPI 异步服务化

---

## 详细版（适合项目 portfolio / 面试自我介绍，~350 字）

### 项目定位

一个面向个人财务管理的 AI Agent，提供收支分析、预算规划、金融文档问答等服务。**项目从头构建，不依赖 LangChain、AutoGen 等主流 Agent 框架**，目的在于深入理解 LLM Agent 的底层运作机制——从 API 调用、工具编排、记忆管理到可观测性，每个环节都是自己实现。

### 核心技术实现

**Agent 架构**：手写 ReAct（Reasoning + Acting）循环，实现了 Think → Act → Observe 的完整推理链路。Agent 支持单轮多工具并发执行（`asyncio.gather`），采用主 Agent 与 Critic 评审器的双客户端分离设计，通过 `functools.partial` 绑定隐藏参数防止 LLM 幻觉注入。

**Agentic RAG**：基于 Qdrant 构建了两阶段检索管道——向量召回（bge-small-zh-v1.5）→ Cross-Encoder 精排（bge-reranker-base），并在此基础上实现了查询路由器（QueryRouter，区分精确查询/综合概述）和检索评审器（RetrievalCritic，双阈值质量检查+自动改写重试）。整个 Agentic 层完全封装在单个工具内部，Agent 主循环无感知。通过 RAGAS 4 指标对 28 组 QA 对进行了 Baseline vs Agentic 的定量 A/B 对比。

**LLM 自我反思**：为预算规划功能实现了双 Critic 反思循环——外部 Critic 审查（建议数值参数调整）+ 自我反思（审视逻辑一致性与目标匹配度），配合 `_compare_plans()` 确定性对比函数选择最优版本。通过 LLM-as-Judge 对 10 个测试用例进行三维盲评（储蓄合理性/可操作性/目标匹配），验证了 Reflection 对输出质量的提升效果。

**可观测性**：实现了零开销的条件追踪层——LangFuse 未配置时 `@traced()` 装饰器返回透传函数，零性能开销、零崩溃风险；配置后提供完整的 Trace → Span → Generation 调用链追踪，所有内容发送前经过 5 条正则规则的 PII 脱敏（邮箱、手机号、身份证号等）。

**工程实践**：55 条 pytest 测试覆盖 14 个纯函数和 5 个 Pydantic 校验器；FastAPI 异步服务化 + 会话管理；Mem0 跨会话记忆（渐进式信息披露架构）；Git Conventional Commits 规范化提交。

### 技术栈

`Python` `Anthropic SDK` `DeepSeek-v4-pro` `Qdrant` `FastEmbed` `FastAPI` `LangFuse` `Mem0` `Pydantic v2` `pytest` `RAGAS`

### 关键数据

| 指标 | 数值 |
|------|------|
| 生产代码 | ~4,800 行 |
| 工具数量 | 8 个 |
| pytest 测试 | 55 条，83 个断言 |
| RAG 评估 QA 对 | 28 组（3 个难度层级） |
| LLM-as-Judge 测试用例 | 10 个（3 维盲评） |
| Pydantic 数据模型 | 10 个（含 field/model validator） |
